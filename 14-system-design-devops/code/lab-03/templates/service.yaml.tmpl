# Catalogue entry. This is how an incident finds a human.
name: ${SERVICE_NAME}
owner: ${OWNER}
tier: ${TIER}
platform-version: ${PLATFORM_VERSION}
repo: https://github.com/example-org/${SERVICE_NAME}
runbook: docs/runbook.md
dependencies: []
